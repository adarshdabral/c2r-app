# Frontend (Expo) environment — copy to `.env`.
# On a physical phone via Expo Go, this must be your dev machine's CURRENT LAN IP
# (not localhost). `npm start` auto-syncs it via scripts/start.mjs.
EXPO_PUBLIC_API_URL=http://192.168.1.100:4000/api
