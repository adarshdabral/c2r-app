import { ProfileScreen } from "@/features/ProfileScreen";

// Shared profile screen (see app/(user)/(tabs)/profile.tsx).
export default function RecyclerProfileScreen() {
  return <ProfileScreen />;
}
